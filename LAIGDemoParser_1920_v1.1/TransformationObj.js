/**
 * TransformationObj class, responsible for parsing XML scene data.
 */
class TransformationObj {

    /**
     * @constructor
     */
	constructor(id, translate) {

        this.id = id;
        this.translate = translate;
        this.rotation = rotation;
        this.scale = scale;
    }
}