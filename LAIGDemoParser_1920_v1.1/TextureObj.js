/**
 * TextureObj class, responsible for parsing XML scene data.
 */
class TextureObj {

    /**
     * @constructor
     */
	constructor(id, textureFile) {

        this.id = id;
        this.textureFile = textureFile;
    }
}